% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sharpapi.R
\name{sharpapi_odds}
\alias{sharpapi_odds}
\title{Current odds as a data frame}
\usage{
sharpapi_odds(...)
}
\arguments{
\item{...}{Filters passed to the API, e.g. sport = "soccer",
league = "mlb", sportsbook = "pinnacle", market_type = "moneyline",
limit = 500.}
}
\value{
A data frame with one row per odds line. Columns include
\code{event_id}, \code{sportsbook}, \code{market_type}, \code{selection}, \code{odds_american},
\code{odds_decimal} and \code{odds_probability}.
}
\description{
Current odds as a data frame
}
\examples{
# Toy example. A canned response stands in for the API, so this block runs
# offline -- no key, no network -- and returns the same shape a live call does.
if (requireNamespace("withr", quietly = TRUE)) {
  json <- '{"data": [
    {"event_id": "evt-1", "sportsbook": "pinnacle",
     "market_type": "moneyline", "selection": "Yankees",
     "odds_american": -145, "odds_decimal": 1.69,
     "odds_probability": 0.5918}
  ]}'
  mock <- function(req) {
    httr2::response(
      status_code = 200L,
      headers = list(`Content-Type` = "application/json"),
      body = charToRaw(json)
    )
  }
  withr::with_envvar(c(SHARPAPI_KEY = "toy-key"), {
    httr2::with_mocked_responses(mock, sharpapi_odds(sport = "baseball", limit = 5))
  })
}

\donttest{
if (nzchar(Sys.getenv("SHARPAPI_KEY"))) {
  odds <- sharpapi_odds(sport = "baseball", limit = 5)
  head(odds)
}
}
}
