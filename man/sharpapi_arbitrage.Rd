% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sharpapi.R
\name{sharpapi_arbitrage}
\alias{sharpapi_arbitrage}
\title{Arbitrage opportunities}
\usage{
sharpapi_arbitrage(...)
}
\arguments{
\item{...}{Filters passed to the API.}
}
\value{
A data frame with one row per arbitrage opportunity. Columns
include \code{event_id}, \code{sport}, \code{market_type} and \code{profit_percent}. The
individual sides live in a nested \code{legs} column rather than as top-level
fields. A key below the Hobby tier raises an HTTP error rather than
returning an empty frame.
}
\description{
Requires a Hobby tier key or higher.
}
\examples{
# Toy example. A canned response stands in for the API, so this block runs
# offline -- no key, no network -- and returns the same shape a live call does.
if (requireNamespace("withr", quietly = TRUE)) {
  json <- '{"data": [
    {"event_id": "evt-1", "sport": "baseball",
     "market_type": "moneyline", "profit_percent": 1.8}
  ]}'
  mock <- function(req) {
    httr2::response(
      status_code = 200L,
      headers = list(`Content-Type` = "application/json"),
      body = charToRaw(json)
    )
  }
  withr::with_envvar(c(SHARPAPI_KEY = "toy-key"), {
    httr2::with_mocked_responses(mock, sharpapi_arbitrage(sport = "baseball"))
  })
}

\donttest{
if (nzchar(Sys.getenv("SHARPAPI_KEY"))) {
  arb <- sharpapi_arbitrage(sport = "baseball")
  head(arb)
}
}
}
